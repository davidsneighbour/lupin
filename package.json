{
  "name": "lupin",
  "version": "0.0.5",
  "description": "Lupin. A swiss army knife for managing my own [GoHugo](https://gohugo.io) based website projects. Might be useful for others one day.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davidsneighbour/lupin.git"
  },
  "author": "Patrick Kollitsch <patrick@davids-neighbour.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/davidsneighbour/lupin/issues"
  },
  "homepage": "https://github.com/davidsneighbour/lupin#readme",
  "devDependencies": {
    "@davidsneighbour/standard-version-config": "^4.0.8",
    "@davidsneighbour/tools": "^4.0.9"
  },
  "scripts": {
    "release": "standard-version --release-as patch -a -t \"v\" --releaseCommitMessageFormat \"chore(release): v{{currentTag}}\" && git push origin --all && git push origin --follow-tags",
    "release:major": "standard-version --release-as major -a -t \"v\" --releaseCommitMessageFormat \"chore(release): v{{currentTag}}\" && git push origin --all && git push origin --follow-tags",
    "release:minor": "standard-version --release-as minor -a -t \"v\" --releaseCommitMessageFormat \"chore(release): v{{currentTag}}\" && git push origin --all && git push origin --follow-tags"
  }
}
